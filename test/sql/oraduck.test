# name: test/sql/oraduck.test
# description: OraDuck loads without Oracle Instant Client (OCI is loaded on first use)
# group: [oraduck]

require oraduck

query I
SELECT count(*) FROM duckdb_functions() WHERE function_name = 'oraduck_oci_version';
----
1
